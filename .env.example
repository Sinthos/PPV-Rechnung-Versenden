# Microsoft Graph API Configuration
# Required: Azure AD App Registration credentials
TENANT_ID=your-tenant-id-here
CLIENT_ID=your-client-id-here
CLIENT_SECRET=your-client-secret-here

# Sender email address (must have SendMail permissions in Azure AD)
SENDER_ADDRESS=rechnung@ppv-web.de

# Application Settings
# Base directory for the application data (SQLite database, logs)
APP_DATA_DIR=/opt/ppv-rechnung/data

# Default source folder for invoice PDFs (can be changed in web UI)
DEFAULT_SOURCE_FOLDER=/Dokumente

# Default target folder for processed invoices (can be changed in web UI)
DEFAULT_TARGET_FOLDER=/Dokumente/RE - Rechnung

# Default daily send time in HH:MM format, Europe/Berlin timezone (can be changed in web UI)
DEFAULT_SEND_TIME=09:00

# Web server settings
HOST=0.0.0.0
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Web UI Basic Auth (empfohlen setzen!)
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# Encrypted environment (optional):
# Set ENV_ENCRYPTION_KEY (Passphrase) and ENV_ENCRYPTION_SALT (Base64, optional)
# and encrypt your .env to .env.enc with: python -m app.env_crypto encrypt .env .env.enc
# Then delete/secure the plaintext .env.
# ENV_ENCRYPTION_KEY= # setzen!
# ENV_ENCRYPTION_SALT= # optional Base64 Salt, z.B. mit openssl rand -base64 16
